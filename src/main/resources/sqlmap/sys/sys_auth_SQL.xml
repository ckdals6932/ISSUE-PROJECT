<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="sys_auth">
	<select id="select_SYS_AUTH" parameterType="hashmap" resultType="hashmap">
		SELECT *
		FROM SYS_AUTH
	</select>
	
	<insert id="insert_SYS_AUTH" parameterType="hashmap">
		BEGIN
		<foreach collection="data_list" item="data" index="index" separator=";">
			INSERT INTO SYS_AUTH (
				AUTH_SEQ
				, AUTH_CD
				, AUTH_NM
				, REG_USER_SEQ
				, REG_DT
			)
			SELECT 
				NEXT VALUE FOR SYS_AUTH_SEQ
				, #{data.auth_cd}
				, #{data.auth_nm}
				, #{login_user_seq}
				, GETDATE()
		</foreach>
		END;
	</insert>
	
	<delete id="delete_SYS_AUTH" parameterType="hashmap">
		DELETE SYS_AUTH
	</delete>
</mapper>